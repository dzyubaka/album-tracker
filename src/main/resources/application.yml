spring:
  application.name: album-tracker
  datasource:
    url: jdbc:postgresql://localhost/album_tracker
    username: album_tracker
  jpa.hibernate.ddl-auto: update
  jackson.default-property-inclusion: non_null
